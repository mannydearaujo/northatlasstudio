PAGESPEED_API_KEY=replace-with-your-pagespeed-insights-api-key

# Path to a GA4/Search Console service-account JSON key. Store the actual key file OUTSIDE
# this repo (e.g. ~/.secrets/) — never inside the project folder.
GA4_SERVICE_ACCOUNT_KEY_PATH=/replace/with/path/outside/this/repo/service-account.json
