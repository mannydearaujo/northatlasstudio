# Vercel
.vercel

# OS
.DS_Store

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Env
.env
.env.local

# Generated audit reports — keep local, never deploy
ai-search-audit-*.html
copy-rewrites-*.md
domain-cutover-checklist.md
launch-qa-*.md

# Generated code graph — keep local, never deploy
graphify-out/
graph.json

# Internal strategy docs — keep local, never deploy
GA4-lead-logic.md
weekly-report-template.md
90-day-rollout-plan.md
ai-search-reference.md
NEXT-STEPS.md
BUSINESS-BLUEPRINT.md
audit-kpi-dashboard-examples.html
scripts/
!scripts/
!scripts/production-sync.mjs
!scripts/pagespeed-insights.mjs
!scripts/sync-agent-docs.mjs
!scripts/ga4-report.mjs
!scripts/search-console-report.mjs
!scripts/signal-registry.json

# Defensive: service-account/credential files should live outside this repo entirely
# (e.g. ~/.secrets/), but ignore them here too in case a copy ever lands in-tree.
*.serviceaccount.json
secrets/
.secrets/

# Design exploration samples — not for deploy
sample-*.html

# Private client workspaces — keep local, never deploy or commit
clients/

# Client site repos living alongside the agency repo (own git remotes)
alpha-gutter/

# Keep shared agency skill library portable and trackable
!agency-skills/
!agency-skills/**

# Brand pack archive — folder is tracked, zip is redundant
North_Atlas_Studio_Brand_Pack.zip

# Raw research inputs — keep local, never deploy
AI Search Readiness Research/

# Rebuilt brand asset pack — interim, untracked until a canonical brand-asset
# source is chosen (see NEXT-STEPS.md top-tier upgrades)
North_Atlas_Studio_REBUILT_Production_Assets/

# AI consulting offer plan — internal, keep local, never deploy
ai-consulting-offer-plan.html
fabio-borges-ig-analysis.xlsx
demo-ai-ops-landing.html
30-day-revenue-sprint.html
